<!DOCTYPE html>
<html lang="en">
<head>
    <title>CodeGrade » Error: {{ error_title | default('Unknown error') }}</title>
    <style>
    body {
        width: 100vw;
        display: flex;
        flex-direction: column;
        text-align: left;
        margin: 0;
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol";
    }

    .header {
        padding: 1rem 2rem;
        display: flex;
        justify-content: space-between;
        background: #f5f5f5;
        box-shadow: 0 0 4px rgba(0, 0, 0, 0.75);
        color: #292b2c;
    }

    .title {
        margin: auto 0;
        padding: 0;
    }

    .logo {
        height: 1.5rem;
        width: auto;
    }
    .alert {
        flex: 1 1 auto;
        line-height: 1.5;
        margin: 1rem;
        border: 1px solid transparent;
        border-radius: 0.25rem;
        padding: 0.75rem 1.25rem;
    }

    .alert-danger {
        color: #721c24;
        background-color: #f8d7da;
        border-color: #f5c6cb
    }
    </style>
</head>
<body>
    <div class="header">
        <h4 class="title">{{ error_title | default('Oops something unknown went wrong&hellip;') }}</h4>
        <a href="/">
            <img class="logo" src="/static/img/codegrade-inv.svg" alt="logo" />
        </a>
    </div>
    <div class="alert alert-danger">
        <p style="margin: 0 auto; text-align: justify;">
            {{ error_message | safe }}
        </p>
    </div>

</body>
</html>
